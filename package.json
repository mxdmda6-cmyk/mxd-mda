{
  "name": "mxd-mda",
  "version": "1.0.0",
  "description": "Interactive Day of the Dead Birthday Card with Chess Utilities",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "node --test tests/**/*.test.js",
    "test:watch": "node --test --watch tests/**/*.test.js"
  },
  "keywords": ["chess", "utilities", "typescript"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^24.10.0"
  }
}
