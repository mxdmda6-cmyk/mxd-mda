# MXD-MDA Bug Fix Log — 2026-06-05 v01

## Status

**Track:** Production spine stabilization  
**Scope:** Repository hygiene, CLI accuracy, CI safety, configuration safety, deployment gates  
**Canon Risk:** Green  
**Live Publishing Risk:** None — no publishing paths enabled

---

## Fixes Applied

| Area | Issue | Correction | Status |
|---|---|---|---|
| Root package metadata | Root `package.json` described an unrelated chess/card utility project. | Rewrote metadata for MXD-MDA transmedia orchestration system. | Fixed |
| Root helper scripts | `npm build` targeted stale TypeScript setup. | Replaced with Python orchestrator helper commands and QA chain. | Fixed |
| Root TypeScript config | Root `tsconfig.json` made the repo appear TypeScript-first even though active system is Python-first. | Removed stale root config. | Fixed |
| Quick Start | Clone URL used placeholder org path. | Replaced with live repo URL. | Fixed |
| Quick Start | Setup implied broader/live integrations were part of the normal startup path. | Clarified local-only startup and gated automation. | Fixed |
| CLI | Executable help text referenced planned commands that did not exist. | Updated usage to active commands only. | Fixed |
| CLI | `test` command name was vague and stale. | Added `doctor` command and kept `test` as backward-compatible alias. | Fixed |
| CI | Workflow relied on smoke checks only. | Added pytest execution. | Fixed |
| CI | Quality workflow could drift because no tool config existed. | Added `pyproject.toml` for pytest, Black, Ruff, and MyPy. | Fixed |
| Secrets template | `.env.example` used realistic fake key formats. | Replaced with safe `replace-me` placeholders. | Fixed |
| Feature flags | Risky integrations needed explicit disabled defaults. | Added tests ensuring risky flags default to `false`. | Fixed |
| Bot deployment | Workflow could be misunderstood as deploy-ready. | Converted to manual dry-run deployment gate only. | Fixed |
| Package marker | `src/__init__.py` contained placeholder text. | Replaced with explicit package docstring. | Fixed |

---

## Tests Added

- `tests/test_orchestrator_cli.py`
  - Validates `version`
  - Validates `doctor`
  - Validates backward-compatible `test` alias
  - Validates `dashboard`

- `tests/test_env_template.py`
  - Prevents realistic-looking fake secrets from returning to `.env.example`
  - Confirms risky feature flags stay disabled by default

---

## Guardrails Reinforced

1. No live publishing path is enabled by default.
2. No bot deployment is triggered by normal push events.
3. No real or realistic-looking secrets belong in templates.
4. CLI commands must reflect implemented behavior, not future promises.
5. Tests should land before automation paths are activated.

---

## Remaining Risks

| Risk | Impact | Next Action |
|---|---|---|
| GitHub Actions status not yet confirmed for latest commits | CI may reveal format/type issues after push processing. | Inspect latest workflow run and patch failures if present. |
| Full `requirements.txt` is broad and integration-heavy | Slow installs and dependency friction. | Keep `requirements-minimal.txt` as default; split future integration requirements by domain. |
| Production dashboard still mostly visual/static | Cannot yet serve as true sprint source of truth. | Add dashboard data model and CLI output contract. |
| Bot directories are not implemented | Deployment gate warns, but bots are not ready. | Keep deployment disabled until bot source, health checks, and rollback exist. |

---

## Decision

This stabilization pass is **good enough to ship** as a foundation correction. The next useful work is not more polishing; it is building the production dashboard data contract and keeping tests around it.
